import React from 'react';
import { ListItemButton, ListItemButtonProps } from '@mui/material';

const TextChannelButton: React.FC<ListItemButtonProps> = (props) => {
  return <ListItemButton {...props} />;
};

export default TextChannelButton; 